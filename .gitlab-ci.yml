image: ubuntu:16.04

stages:
  - build
  - deploy

build-project-image:
  stage: build
  image: docker:git
  services:
  - docker:dind
  script:
    - docker login -u gitlab-ci-token -p $CI_BUILD_TOKEN registry.gitlab.com
    - docker build -t registry.gitlab.com/zonaster/MAA .
    - docker push registry.gitlab.com/zonaster/MAA:latest
  only:
    - master


deploy-to-production:
  stage: deploy
  before_script:
    - 'which ssh-agent || ( apt-get update -y && apt-get install openssh-client -y )'
    - eval $(ssh-agent -s)
    - ssh-add <(echo "$DEPLOY_KEY" | base64 -d)
    - mkdir -p ~/.ssh
    - '[[ -f /.dockerenv ]] && echo -e "Host *\n\tStrictHostKeyChecking no\n\n" > ~/.ssh/config'
  script:
    - ssh host@35.231.222.250 'docker pull registry.gitlab.com/zonaster/MAA && docker stop MAA && docker rm MAA && docker run --name=MAA --restart=always -p 8090:80 -d registry.gitlab.com/zonaster/MAA:latest'
  only:
    - master
